type: custom:auto-entities
show_empty: false
card:
  type: glance
  title: BBQ Cleanup
  show_header_toggle: false
  show_state: false
filter:
  include:
    - entity_id: "input_boolean.bbq_*"
      state: "off"
      options:
        tap_action:
          action: toggle
